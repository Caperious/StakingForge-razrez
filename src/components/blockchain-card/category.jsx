export const Category = ({category}) => {
  return <span className={'text-xs text-center text-white font-light bg-primary-secondary-dark p-0.5 mt-0.5 px-2 rounded-full break-keep text-nowrap'}>{category}</span>
}